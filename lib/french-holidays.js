(function() {
  var FrHolidays, moment;

  moment = require('moment');

  Holidays = {
    by_year: function(year) {
      return [
        
      ];
    }
  };

  module.exports = Holidays;

}).call(this);
