#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ical2json/bin/ical2json" "$@"
  ret=$?
else 
  node  "$basedir/../ical2json/bin/ical2json" "$@"
  ret=$?
fi
exit $ret
